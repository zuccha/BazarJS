import { Center, Heading } from '@chakra-ui/react';
import { ReactElement } from 'react';

export default function ProjectCreationFromSource(): ReactElement {
  return (
    <Center flex={1}>
      <Heading>Project creation from source</Heading>
    </Center>
  );
}
